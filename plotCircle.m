function h = plotCircle(x,y,r)
    % plots a circle in the plot that is opened
    hold on
    th = 0:pi/50:2*pi;
    xunit = r * cos(th) + x;
    yunit = r * sin(th) + y;
    h = plot(xunit, yunit);
    hold off
end
